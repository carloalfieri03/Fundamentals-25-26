defaults:
  - _self_ 
  - net: default 

seed: 42

net:
  #depth: 3
  #width: 64
  num_classes: 6

trainer:
  max_epochs: 100
  log_every_n_steps: 10

dataset:
  data_dir: "${oc.env:PWD}/data"
  batch_size: 32
  val_split: 0.2

wandb :
  project: "FDS_PROJECT"
  entity: "catairu-sapienza-universit-di-roma"
  name: "skif2_provaCA"